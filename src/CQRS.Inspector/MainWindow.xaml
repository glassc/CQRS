<Window x:Class="CQRS.Inspector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  Title="MainWindow" Height="350" Width="781">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Row="0" Grid.Column="0" ItemsSource="{Binding Path=Events}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding SequenceNumber}"/>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedTimeStamp}"/>
                <DataGridTextColumn Header="Event" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <GridSplitter Width="5" Grid.Column="1" Grid.Row="0" ResizeBehavior="PreviousAndNext" />

        <DataGrid Grid.Row="0" Grid.Column="2" AutoGenerateColumns="False" >
        </DataGrid>



    </Grid>
</Window>
